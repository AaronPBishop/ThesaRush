import { useState, useEffect } from 'react';

import './styles.css';

const Trophy = ({ trophyType }) => {
    useEffect(() => {

    }, []);
    return (
        <div>
            {trophyType}
        </div>
    );
};

export default Trophy;